import { Layout } from "../components/Layout";

type Props = {};
export const HomePage = ({}: Props) => {
  return (
    <Layout>
      <div>
        <p>HOME PAGE</p>
      </div>
    </Layout>
  );
};
